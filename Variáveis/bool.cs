
//true or false

bool isComplete = false;

isComplete = isComplete;
isComplete = !isComplete;
// usar o ! "flipa" a informação, de falso pra true e true pra falso
// ! pode ser lido também como "is not" ou "não é"

Console.WriteLine(isComplete);
Console.WriteLine(!isComplete);
